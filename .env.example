# (future) Conway automaton API endpoint
# VITE_CONWAY_API_URL=

# Prime's public Solana wallet address. Never put a private key here.
VITE_PRIME_SOLANA_WALLET_ADDRESS=

# The grant proof flow defaults to devnet so it cannot spend real funds.
VITE_SOLANA_CLUSTER=devnet
VITE_SOLANA_RPC_URL=https://api.devnet.solana.com
